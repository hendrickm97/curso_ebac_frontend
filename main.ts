function calculoDaArea(base: number, altura: number): number {
  return base * altura;
}

function saudacao(nome: string): string {
  return `Olá ${nome}`;
}

console.log(calculoDaArea(15, 20));
console.log(saudacao("João"));
